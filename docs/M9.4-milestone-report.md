# Milestone M9.4: Legacy-12 Parity

Status: Complete

M9.4 adds a stable repository-owned Todo target and migrates the legacy twelve
scenarios into `fixtures/legacy-todo/test-plan.json`. The plan uses stable
Requirement references, isolated setup/cleanup, and the formal M9.3 runner.

The migration covers not-found, title validation, summary, create/refresh,
completed state, delete, priority enum, detail, PATCH CORS, PATCH persistence,
search, and count semantics.

Acceptance:

- `npm run verify:m9:legacy-12` - 11 passed, 0 failed
- Exactly 12 cases execute.
- UI, API, and Hybrid evidence are case-scoped.
- Mutating cases clean up successfully.
- Randomized order and repeated runs produce equivalent results with no dirty data.
