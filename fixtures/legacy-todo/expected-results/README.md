# Legacy Todo Golden Results

The M9.4 verifier records the per-case result and evidence manifest for each
fresh target run. The semantic golden is the case map and requirement list;
volatile execution IDs and artifact handles are intentionally not checked in.
